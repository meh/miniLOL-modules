<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE module [
    <!ELEMENT module (name, version, license, description, author, dependencies, contents)>

    <!ELEMENT name (PCDATA)>

    <!ELEMENT version (release, api, stability)>

    <!ELEMENT stability (release, api)>

    <!ELEMENT release (PCDATA)>

    <!ELEMENT api (PCDATA)>

    <!ELEMENT dependencies (dependency*)>

    <!ELEMENT dependency EMPTY>
    <!ATTLIST dependency
        name    CDATA   #REQUIRED
        version CDATA   #REQUIRED
    >

    <!ELEMENT contents (directory*, file*)>
    
    <!ELEMENT directory (directory*, file*)>
    <!ATTLIST directory
        name    CDATA   #REQUIRED
    >

    <!ELEMENT file EMPTY>
    <!ATTLIST file
        name    CDATA   #REQUIRED
        md5sum  CDATA   #REQUIRED
    >
]>

<module>
    <name>IHM</name>

    <version>
        <release>0.2</release>
        <api>0.2</api>

        <stability>
            <release>stable</release>
            <api>stable</api>
        </stability>
    </version>

    <license uri="http://sam.zoy.org/wtfpl/">Do What The Fuck You Want To Public License</license>

    <description>Server side modules infrastructure.</description>

    <author>
        <name>meh.</name>
        <email>meh.ffff@gmail.com</email>
        <homepage>http://meh.doesntexist.org</homepage>
    </author>

    <dependencies></dependencies>

    <contents>
        <file name="main.js" md5sum="d03d47aa51c485f923a93bf2a6fdcc04"/>

        <directory name="system">
            <file name="IHM.js" md5sum="41607860af4223ccbed5c697f8f3d4e3"/>
        </directory>

        <directory name="resources">
            <file name="hatred.xml" md5sum="16b15dfb0d9aa7abfd3a1e8f591c37e0"/>
        </directory>
    </contents>
</module>
